package org.example.repository;

import org.example.model.TariffBranded;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TariffBrandedRepository extends JpaRepository<TariffBranded, Long> {
}